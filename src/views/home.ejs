<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="stylesheet" href="/style/index.css" />
		<title>home - country recipe</title>
	</head>
	<body>
		<header>
			<nav>
				<a href="">home</a>
			</nav>
		</header>

		<main id="home">
			<% for(data of datas){ %>

			<article>
				<header>
					<h2><%= data.label%></h2>
					<% if(data.picture?.flag) {%>
					<div class="flag">
						<img
							src="/images/flag/<%= data.picture.flag%>"
							alt=""
						/>
					</div>
					<% } %>
				</header>

				<p>Culinary speciality : <%= data.recipe %></p>
				<% if(data.picture?.country) {%>
				    <img src="/images/<%= data.picture.country%>" alt="" />
				<% } %>

				<a href="/country/<%= data.id%>" class="cta link">Read more</a>

			</article>

			<hr />
            
			<% } %>
		</main>
		<footer>
			<a
				href="https://www.flaticon.com/free-icons/france"
				title="france icons"
				>France icons created by Freepik - Flaticon</a
			>
		</footer>
	</body>
</html>
